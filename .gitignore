# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# media — mp4 served from R2 (excluded), webm served via Static Assets (committed)
*.mp4

# local backups & state
*.old-acquisition
.omc/
